For an N items array with all keys identical, selection sort uses N(N-1)/2 compares and N exchanges.
For an N items array with all keys identical, insertion sort uses N-1 compares and 0 exchanges.
So, insertion sort is faster.